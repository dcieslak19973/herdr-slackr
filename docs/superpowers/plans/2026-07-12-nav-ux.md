# Navigation + Threads UX Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Newest-at-bottom everywhere, real navigation keys, a new-arrivals indicator, discoverable thread expansion with feedback, and reply-activity rows in the timeline.

**Architecture:** Per `docs/superpowers/specs/2026-07-12-nav-and-threads-ux-design.md`. Task 1 = §1-§4 (ordering + keys + indicator + stickiness); Task 2 = §5-§6 (thread UX); Task 3 = docs + 0.1.4.

**Tech Stack:** Existing crate, closed dep list.

## Global Constraints

- Spec above is the contract; all prior invariants bind. House gate per commit: clippy pedantic `-D warnings`; `cargo test --all-features` (272 today); `cargo fmt --all` + discard CRLF-only rewrites; dense doc comments; cursor/tab/view mutations only via the sanctioned App methods. Branch `nav-ux`. Trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Ordering-dependent existing tests: update EXPECTATIONS to the new ascending order while preserving each test's meaning — never delete a behavior assertion to make it pass.

---

### Task 1: Direction unification, nav keys, indicator, stickiness

**Files:**
- Modify: `src/app.rs` (mention_rows + thread_rows ordering → ascending (§1); `pub fn jump_newest()`, `pub fn jump_first()`, `pub fn page_move(delta_pages: f32)`-style methods over `move_cursor` — page height injected from the event loop's known viewport rows (thread a `viewport_rows: usize` setter or pass as arg, implementer's call, documented; clamped); new-arrivals counter: increments when an append lands while cursor not on last row, cleared when cursor reaches last row (§3-§4; pure transitions tested), exposed as `pub fn pending_new(&self) -> usize`), `src/ui.rs` (bottom-edge `↓ n new` overlay in the muted accent when pending_new > 0; hint text updates), `src/lib.rs` (keys: `G`/End → jump_newest, `g`/Home → jump_first, PageDown/PageUp → page_move(±1.0), ctrl-d/ctrl-u → page_move(±0.5); pass viewport rows).
- Test: pure ordering tests updated (meaning preserved); indicator transition table (arrive-at-bottom → follow + no count; arrive-scrolled-up → count+1; reach bottom → 0); page clamp; render: indicator shows/hides, G jumps.

- [ ] TDD; full gate; commit `feat: unified newest-at-bottom ordering, nav keys, arrivals indicator`.

---

### Task 2: Thread expansion UX + timeline activity rows

**Files:**
- Modify: `src/app.rs` (`toggle_expand_or_read`: a Feed-tab Enter on a thread ROOT row (has marker state) routes to the same expand path as the marker row — timeline and threads view; completion status lines `thread expanded — n replies` / `thread collapsed`; `feed_rows_with_ids`: a reply whose root is known AND collapsed emits an activity row `RowKind`-wise — reuse/extend the orphan inline treatment into a general `ReplyActivity` presentation (text `↳ @author replied: <text>`; keep RowKind::Message with the reply's identity so Enter targets resolve — Enter on an activity row expands the ROOT thread via thread_root_ts, which already walks reply→root); expanded threads: replies nest under root as today and emit NO activity rows), `src/ui.rs` (footer hint `enter expand/collapse thread` when the selected row is root-with-thread/marker/reply/activity; activity-row styling = existing reply styling), `src/lib.rs` (no key changes).
- Test: pure projection tests (collapsed → activity rows at reply positions; expanded → nested only; orphan unchanged; enter-on-root toggles same as marker; enter-on-activity expands root); render: activity row, footer hint variants, status feedback strings.

- [ ] TDD; full gate; commit `feat: discoverable thread expansion and timeline reply activity`.

---

### Task 3: Docs + 0.1.4 release prep

- Modify: `README.md` (Controls: G/g/End/Home/PageUp/Down/ctrl-d/u; navigation section: newest-at-bottom everywhere, the `↓ n new` indicator, follow rules; threads section: enter-anywhere expansion, activity rows), `specs/pane.md` (ordering, keys, indicator, activity-row + expansion contract rows), `CHANGELOG.md` ([0.1.4] user-visible voice incl. the BREAKING-ish note that Mentions/Threads now read top-to-bottom chronological), versions → 0.1.4 (3 files + lock via cargo build). Docs must match landed code.
- [ ] Full gate; commit `docs: navigation and thread UX; release 0.1.4 prep`.

---

## Self-Review Notes
- Spec §1→T1, §2→T1, §3→T1, §4→T1, §5→T2, §6→T2, docs→T3. Non-goals held (no config knobs, no CLI change).
- Type consistency: jump_newest/jump_first/page_move/pending_new defined T1, hinted T3; activity-row projection defined T2 and documented T3.
